# V4.5 全量验收回归结论

- 日期：2026-08-26
- 范围：主 PRD V4.5 §12 的 AC-01～56、我的工作 PRD V1.1 §13 的 MW-01～24、协作关系 PRD V1.1 §13 的 CR-01～23
- 对应 issue：#42（前置返工 #31～#41、#43 全部关闭）

## 结论

三份验收表共 103 条（AC 56 + MW 24 + CR 23），逐条核对完毕：

- **101 条通过**，其中 MW-14、MW-15 两条本轮核对时发现不达标，已在本票修复并补测试；
- **2 条未通过**：MW-09（待我接收组恒空，接收方未建模）、MW-13（等待他人的提醒只覆盖已成卡点的情形）——两条已由 #44、#45 于同日补齐，见「缺口去向」；
- 另有 1 项不带验收编号的缺口：时间型卡点没有动态留痕。

验证门槛全绿：

- `go build ./... && go vet ./... && go test ./...` —— 472 项通过
- `npm run build`（含 tsc） —— 通过

自动化证据分两层：业务规则在 `server/internal/domain/` 的表驱动单测里逐条覆盖，端到端流程在
`server/internal/api/integration_test.go` 用 httptest + Docker 真 Postgres 覆盖。纯视觉与交互条目
没有自动化证据，靠逐页浏览器冒烟核对，核对记录见各自 ticket 的关闭评论。

## 本轮回归发现并修复的问题

| 编号 | 问题 | 修复 |
|---|---|---|
| MW-14 | 任务取消后，该任务的入池审批件、关键字段变更单、完成申请与输入请求仍留在「我的工作」里——派生时只按事项自身状态过滤，没有跟着任务终态收口（卡点侧因为「执行中才派生」侥幸没受影响） | `MyWork` 先算出终态任务集合，审批件与输入请求一并按它过滤；补 `TestMyWorkDropsCancelledTaskItems` |
| MW-15 | 五组固定排序（超期→今天到期→时间升序→无时间按等待时长降序→被阻塞沉一档）**完全没有实现**，卡片按派生顺序输出 | 新增 `sortWorkGroups`，按模块 PRD §7.1 的四级紧急度加「被阻塞沉一档」次级键；补 `TestMyWorkSorting`、`TestMyWorkSortingWithoutDueDate` |

## 遗留缺口

本节记录 2026-08-26 回归当时的状态；三条缺口已分别开票并在同日全部补齐，状态见「缺口去向」。

| 编号 | 缺口 | 出处与去向 |
|---|---|---|
| MW-09 | 「待我接收」组恒为空——**接收方与接收记录尚未建模**，终审通过后不产生待接收项 | `domain/mywork.go` 里已注明；PRD §4.1 有「接收记录」对象，需要独立 ticket 建模（表 + 契约 + 终审后写入 + 确认接收动作） |
| MW-13 | 「等待他人」组的一键提醒只覆盖到该任务已派生出卡点的情形；尚未成卡点的等待没有可寻址的提醒目标 | #38 已写明。提醒端点按卡点合成键寻址，要补需要先给提醒目标独立建模 |
| — | 时间型卡点（审批超时 N×24h、任务超期）没有「何时出现」的任务动态留痕 | ADR 0002「影响」已写明；ADR 0001 规划的每小时 ticker 尚未实现。界面上这两类卡点始终读时派生、显示正确 |

### 缺口去向（2026-08-26 同日补齐）

| 编号 | 票 | 落地 |
|---|---|---|
| MW-09 | #44 | 迁移 00022 建接收方与接收记录；终审通过逐人生成待接收项，任务概况「成果接收」区块确认接收（d2a133c） |
| MW-13 | #45 | `domain/remind.go` 给提醒目标独立建模，等待他人事项按 `wait:<类型>:<ID>` 寻址；迁移 00023 落冷却（0bfcf92） |
| 时间型卡点留痕 | #46 | 进程内每小时 ticker 补记，时间戳取真实发生时刻；迁移 00024 的 `blocker_key` 唯一键防重记（162a00b） |

另有两处**相对原型的有意偏差**（不算缺口，已在对应 issue 写明）：

- 项目总览 overview-brief 头卡去掉了原型的「活跃任务」列——AC-05 要求概览首层不出现任务与进度数字（#41）；
- 项目设置只列「成员与职责」「系统权限」两节，原型另有进度权重、提醒规则、导入记录、操作审计四节，本版没有对应数据模型（#41）。

## 只靠浏览器冒烟核对的条目

下列条目是纯前端渲染或交互，没有服务端可断言的行为，逐页冒烟核对通过；CLAUDE.md 提到的
Playwright 冒烟脚本仍未搭建，是这批条目自动化的下一步。

| 范围 | 编号 | 核对记录 |
|---|---|---|
| 图谱与关系列表 | AC-08、AC-09、AC-27、AC-41～47、AC-49、CR-01～07、CR-11、CR-13～15、CR-17～23 | #40 关闭评论（层级视图、KR 任务关系层、详情面板开关、列表切换、全局展开）；数据供给侧由 AC-07／10／11／28／48 的测试覆盖 |
| 任务详情视觉 | AC-31、AC-51、AC-52、AC-56 | #35、#36 关闭评论；四 Tab 顺序、任务概况五块顺序与协作关系空态由 #47 冒烟核对 |
| 全部任务视觉 | AC-54 | #39 关闭评论 |
| 我的工作视觉 | MW-17、MW-22、MW-23、MW-24 | #38、#41 关闭评论（含 `from=pending` 时进度可编辑、其他来源只读的两侧验证） |

## 覆盖矩阵

### AC-01～AC-56（主 PRD V4.5 §12）

| 编号 | 自动化证据（Go 测试函数） |
|---|---|
| AC-01 | TestOkrTableBatchCreate、TestProjectMembersAndPermissions |
| AC-02 | TestImportAndBatchPool、TestReportExport |
| AC-03 | TestCanInviteForKr、TestCanRevokeInvite、TestFulfillInvite、TestTaskCreateAndPoolReview、TestTaskInviteLifecycle |
| AC-04 | TestApprovalWaitingLabel、TestCanSubmitPoolReview、TestDecidePoolReview、TestMyWorkFiveGroups、TestMyWorkGrouping、TestMyWorkWaitingApprovalCopy、TestOkrTableBatchCreate、TestProjectReport、TestReviewStateLabels、TestStageLabel、TestStatusLabel、TestSubmitPoolReview、TestTaskCreateAndPoolReview、TestTaskCreationOutcome、TestTaskDetail、TestValidateNewTask |
| AC-05 | TestKrRiskNote、TestMyWorkWaitingApprovalCopy |
| AC-06 | TestOkrTableBatchCreate、TestProjectMembersAndPermissions、TestTaskInviteLifecycle、TestTaskStatusAndProgress |
| AC-07 | TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness |
| AC-08 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-09 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-10 | TestAnalyzeHardEdges、TestInterlockAndCriticalPath、TestMyWorkFiveGroups |
| AC-11 | TestBlockerKindLabel、TestCanRemindBlocker、TestDeriveBlockersEntryAndExit、TestDerivedBlockersAndRemind、TestMultiSourceInputs、TestMyWorkBlockerStageUsesKindLabel、TestMyWorkFiveGroups、TestProjectReport、TestReportRangeFrom |
| AC-12 | TestCanUpdateProgress、TestProgressCoverage、TestTaskInviteLifecycle、TestTaskStatusAndProgress、TestValidateProgress |
| AC-13 | TestCanManageReviewers、TestCompletionReviewFlow、TestDiscussionsAndNotifications、TestSubmitCompletionOutcome、TestSubmitCompletionRule |
| AC-14 | TestCanManageReviewers、TestDecideIntermediateRule、TestDeliverableEdgesAndReadiness、TestIntermediateReviewOrSign、TestSubmitCompletionOutcome |
| AC-15 | TestCanSubmitCompletion、TestCompletionReviewFlow、TestDecideCompletionRule、TestDiscussionsAndNotifications |
| AC-16 | TestDerivedBlockersAndRemind、TestMyWorkFiveGroups、TestMyWorkGrouping |
| AC-17 | TestArtifactsAndPackages、TestInterlockAndCriticalPath |
| AC-18 | TestArtifactsAndPackages、TestInterlockAndCriticalPath、TestValidatePackage |
| AC-19 | TestArtifactsAndPackages、TestKrRiskNote、TestProjectReport、TestReportRangeFrom |
| AC-20 | TestProjectReport、TestReportExport |
| AC-21 | TestAuthAndProjectsEndToEnd、TestImportAndBatchPool、TestProjectMembersAndPermissions、TestUnifiedPermissionsAcrossIdentities |
| AC-22 | TestAuthAndProjectsEndToEnd、TestImportAndBatchPool、TestProjectMembersAndPermissions、TestUnifiedPermissionsAcrossIdentities |
| AC-23 | TestDecideFieldChangeRule、TestFieldChangeApproval、TestFieldChangeRoute、TestTaskDetail、TestValidateKeyFieldChanges |
| AC-24 | TestDecideIntermediateRule、TestDeliverableEdgesAndReadiness、TestIntermediateReviewOrSign、TestSubmitCompletionOutcome |
| AC-25 | TestImportAndBatchPool、TestReportExport |
| AC-26 | TestCanCreateTask、TestOkrTableBatchCreate、TestTaskCreateAndPoolReview、TestTaskCreationOutcome、TestValidateNewTask |
| AC-27 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-28 | TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness、TestValidateNewEdge |
| AC-29 | TestIntermediateReviewOrSign、TestMemberInputRequests、TestValidateMemberInput |
| AC-30 | TestAcceptInputRule、TestIntermediateReviewOrSign、TestMemberInputRequests、TestProvideInputRule、TestValidateMemberInput |
| AC-31 | TestCurrentStage、TestReviewStateLabels、TestStageLabel、TestTaskDetail、TestTaskStatusAndProgress；四 Tab 与五块顺序见下节「只靠浏览器冒烟核对的条目」 |
| AC-32 | TestDeliverablesAndFiles、TestFieldChangeApproval |
| AC-33 | TestDeliverablesAndFiles、TestFieldChangeApproval |
| AC-34 | TestTaskDetail、TestTaskStatusAndProgress |
| AC-35 | TestCanDiscuss、TestDeliverablesAndFiles、TestDiscussionsAndNotifications |
| AC-36 | TestDeliverablesAndFiles、TestDiscussionNotifyTargets、TestDiscussionsAndNotifications、TestValidateDiscussion |
| AC-37 | TestDecideIntermediateRule、TestDeliverableEdgesAndReadiness、TestIntermediateReviewOrSign、TestSubmitCompletionOutcome |
| AC-38 | TestCanSubmitCompletion、TestCompletionReviewFlow、TestDecideCompletionRule、TestDiscussionsAndNotifications |
| AC-39 | TestCompletionReviewFlow、TestDiscussionsAndNotifications |
| AC-40 | TestCompletionReviewFlow、TestDiscussionsAndNotifications |
| AC-41 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-42 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-43 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-44 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-45 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-46 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-47 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-48 | TestCanConfigureInputs、TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness、TestEdgeReady |
| AC-49 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-50 | TestDeliverableEdgesAndReadiness；空区块不显示与协作关系 Tab 空态见下节「只靠浏览器冒烟核对的条目」 |
| AC-51 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-52 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-53 | TestDeriveDisplayStatus、TestFirstUnmetRequiredInput、TestMemberEdgeReady、TestMemberInputRequests、TestMultiSourceInputs、TestValidateMemberInputs、TestValidateNewTaskInputs |
| AC-54 | —（见下节「只靠浏览器冒烟核对的条目」） |
| AC-55 | TestMyWorkCardActionAndRemind |
| AC-56 | —（见下节「只靠浏览器冒烟核对的条目」） |

### MW-01～MW-24（我的工作 PRD V1.1 §13）

| 编号 | 自动化证据（Go 测试函数） |
|---|---|
| MW-01 | TestDerivedBlockersAndRemind、TestMyWorkFiveGroups、TestMyWorkGrouping |
| MW-02 | TestCompletionReviewFlow、TestDiscussionsAndNotifications、TestMyWorkGrouping |
| MW-03 | TestCompletionReviewFlow、TestDiscussionsAndNotifications |
| MW-04 | TestDerivedBlockersAndRemind、TestMyWorkFiveGroups、TestMyWorkGrouping |
| MW-05 | TestMyWorkGrouping |
| MW-06 | TestMyWorkGrouping |
| MW-07 | TestDeliverableEdgesAndReadiness、TestIntermediateReviewOrSign、TestMyWorkGrouping |
| MW-08 | TestDerivedBlockersAndRemind、TestMyWorkFiveGroups、TestMyWorkGrouping |
| MW-09 | TestDerivedBlockersAndRemind、TestMyWorkFiveGroups、TestMyWorkGrouping |
| MW-10 | TestIntermediateReviewOrSign、TestMemberInputRequests、TestMyWorkGrouping |
| MW-11 | TestIntermediateReviewOrSign、TestMemberInputRequests、TestMyWorkGrouping |
| MW-12 | TestBlockerActivityDiff、TestDerivedBlockersAndRemind、TestMultiSourceInputs、TestMyWorkBlockerStageUsesKindLabel、TestMyWorkFiveGroups、TestMyWorkGrouping、TestReportRangeFrom |
| MW-13 | TestDerivedBlockersAndRemind、TestMultiSourceInputs、TestMyWorkCardActionAndRemind |
| MW-14 | TestMyWorkDropsCancelledTaskItems |
| MW-15 | TestMyWorkDropsCancelledTaskItems、TestMyWorkSorting、TestMyWorkSortingWithoutDueDate |
| MW-16 | TestImportAndBatchPool、TestUnifiedPermissionsAcrossIdentities |
| MW-17 | —（见下节「只靠浏览器冒烟核对的条目」） |
| MW-18 | TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness、TestDiscussionsAndNotifications、TestIntermediateReviewOrSign、TestLoginRateLimit、TestTaskActivity |
| MW-19 | TestMyWorkGrouping、TestTaskCreateAndPoolReview、TestTaskInviteLifecycle |
| MW-20 | TestMyWorkGrouping |
| MW-21 | TestMyWorkCardActionAndRemind |
| MW-22 | —（见下节「只靠浏览器冒烟核对的条目」） |
| MW-23 | —（见下节「只靠浏览器冒烟核对的条目」） |
| MW-24 | —（见下节「只靠浏览器冒烟核对的条目」） |

### CR-01～CR-23（协作关系 PRD V1.1 §13）

| 编号 | 自动化证据（Go 测试函数） |
|---|---|
| CR-01 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-02 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-03 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-04 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-05 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-06 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-07 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-08 | TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness |
| CR-09 | TestInterlockAndCriticalPath、TestMyWorkFiveGroups |
| CR-10 | TestInterlockAndCriticalPath、TestMyWorkFiveGroups |
| CR-11 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-12 | TestCompletionReviewFlow、TestDeliverableEdgesAndReadiness |
| CR-13 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-14 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-15 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-16 | TestDerivedBlockersAndRemind、TestMultiSourceInputs |
| CR-17 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-18 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-19 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-20 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-21 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-22 | —（见下节「只靠浏览器冒烟核对的条目」） |
| CR-23 | —（见下节「只靠浏览器冒烟核对的条目」） |

## 复现方式

```
docker compose up -d postgres
cd server && go build ./... && go vet ./... && go test ./...
cd ../web && npm run build
```

集成测试自带 goose 迁移，跑前不需要手工建库。
